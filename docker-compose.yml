services:
  validator:
    build: .
    env_file: .env
    restart: unless-stopped
    user: 1000:1000
    stdin_open: true
    tty: true
    volumes:
      - ./output:/app/output
      - ./cache:/app/cache
